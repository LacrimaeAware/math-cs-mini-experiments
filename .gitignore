# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.mypy_cache/
.ipynb_checkpoints/

# --- Virtual environment (managed by uv; recreate with `uv sync`) ---
.venv/
venv/

# --- IDE / editor ---
.idea/
.vscode/

# --- OS cruft ---
.DS_Store
Thumbs.db

# --- Private notes: never committed (raw research archive, informal logs) ---
private/

# --- Temp helper scripts (root-level, prefixed with _) ---
/_*.py

# Note: outputs/ is intentionally tracked: the heatmaps, the training video and
# the run data are kept as a record / gallery of past experiments.
